# =============================================================================
# AGiXT Server Configuration - MINIMAL BASÉ ISO
# =============================================================================
# File: AGIXT_0530_1239_1.env
# Purpose: Configuration minimale basée sur l'ISO qui fonctionne
# Usage: curl -H "Authorization: token YOUR_GITHUB_TOKEN" -sSL https://raw.githubusercontent.com/mocher01/agixt-configs/main/install-agixt.py | python3 - AGIXT_0530_1239 YOUR_GITHUB_TOKEN
# =============================================================================

# =============================================================================
# CONFIGURATION EXACTE DE L'ISO (QUI FONCTIONNE)
# =============================================================================
AGIXT_AUTO_UPDATE=true
AGIXT_API_KEY=
UVICORN_WORKERS=10
WORKING_DIRECTORY=./WORKSPACE
TEXTGEN_URI=http://text-generation-webui:5000
AGIXT_URI=http://agixt:7437
TZ=Europe/Paris

# =============================================================================
# AI PROVIDERS (Optionnel - laissé vide pour test initial)
# =============================================================================
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# =============================================================================
# NOTES IMPORTANTES
# =============================================================================
# 1. 🔑 Configuration EXACTEMENT basée sur l'ISO qui fonctionne
# 2. 🚫 AUCUNE variable d'auth forcée (AGIXT_REQUIRE_API_KEY supprimée)
# 3. 🌐 AGIXT_URI en interne Docker (http://agixt:7437) comme l'ISO
# 4. 🎯 Test avec configuration minimale avant ajout de fonctionnalités
# 5. ✅ Toutes variables non-ISO supprimées pour test initial
